## Setting up
```bash
cd nodejs-server-boilerplate

# set environment variables
cp .env.example .env # don't forget to change the values

# install dependencies
yarn

# create database and tables
yarn migrate

# start server
yarn dev
```
